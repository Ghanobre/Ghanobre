jobs:
  hello-world:
    runs-on: self-hosted
    steps:
      - name: Sanity
        run: |
          set -x
          whoami; uname -a; which git; which tar; which unzip
          git --version
          df -h | head

      - name: Checkout
        uses: actions/checkout@v4

      - name: OK
        run: echo "WSL runner OK ✅"
